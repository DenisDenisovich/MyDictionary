<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/group_menu_group_base"
        android:visible="false">
        <item
            android:id="@+id/group_menu_add"
            android:title="add"
            android:icon="@drawable/ic_word_add"
            app:showAsAction="always"/>
    </group>
    <group
        android:id="@+id/group_menu_group_context"
        android:visible="true">
        <item
            android:id="@+id/group_menu_delete"
            android:title="@string/word_menu_delete"
            android:icon="@drawable/ic_delete_black_24dp"
            app:showAsAction="always">
        </item>
        <item
            android:id="@+id/group_menu_edit"
            android:title="@string/word_menu_edit"
            app:showAsAction="never">
        </item>

    </group>


</menu>